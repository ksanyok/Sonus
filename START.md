# 🚀 Быстрый старт

## Приложение уже запущено!

Сервер работает на: **http://127.0.0.1:8000**

## Что нужно сделать дальше:

### 1. Настройте OpenAI API Key

Откройте файл `.env` и замените:
```
OPENAI_API_KEY=sk-your-key-here
```
на ваш реальный ключ от OpenAI.

### 2. Откройте приложение

Откройте в браузере: http://127.0.0.1:8000

### 3. Загрузите аудио

- Нажмите "Выбрать файл" в разделе "Аудиофайл"
- Выберите MP3 или WAV файл
- (Опционально) Загрузите Excel рубрику
- Нажмите "Анализировать"

## Тестовый файл

Для тестирования создан файл: `storage/uploads/audio/test-call.wav`

## Управление сервером

### Остановить сервер:
```bash
lsof -ti:8000 | xargs kill -9
```

### Перезапустить сервер:
```bash
cd /Users/oleksandr/Documents/GitHub/pristav/call-audit-proto
php -S 127.0.0.1:8000 -t public
```

### Просмотр логов:
```bash
tail -f /tmp/php-server.log
```

## API Примеры

### Health Check
```bash
curl http://127.0.0.1:8000/healthz
```

### Создать анализ (требует OPENAI_API_KEY)
```bash
curl -X POST http://127.0.0.1:8000/api/calls \
  -F "audio=@path/to/your/audio.mp3" \
  -F "lang=auto"
```

## Troubleshooting

**Ошибка "Address already in use":**
```bash
lsof -ti:8000 | xargs kill -9
```

**Ошибка при анализе:**
- Проверьте OPENAI_API_KEY в .env
- Убедитесь, что файл аудио корректный
- Проверьте логи: `tail -f /tmp/php-server.log`

---

**Готово!** 🎉 Приложение работает и готово к использованию!
